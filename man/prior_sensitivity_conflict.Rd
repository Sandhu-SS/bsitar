% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-helper-31.R
\name{prior_sensitivity_conflict.bgmfit}
\alias{prior_sensitivity_conflict.bgmfit}
\alias{prior_sensitivity_conflict}
\title{Flag parameters with possible prior-data conflict}
\usage{
\method{prior_sensitivity_conflict}{bgmfit}(
  x,
  threshold_prior = 0.05,
  threshold_lik = 0.05,
  ...
)

prior_sensitivity_conflict(x, ...)
}
\arguments{
\item{x}{An object returned by \code{\link[=prior_sensitivity]{prior_sensitivity()}}.}

\item{threshold_prior}{Numeric threshold used to flag prior sensitivity.}

\item{threshold_lik}{Numeric threshold used to flag likelihood sensitivity.}

\item{...}{Ignored.}
}
\value{
A list with components:
\describe{
\item{conflict_params}{Character vector of variables flagged for both
prior and likelihood sensitivity.}
\item{prior_only_params}{Character vector of variables flagged only for
prior sensitivity.}
\item{prior_flagged}{Character vector of all variables flagged for
prior sensitivity.}
\item{likelihood_flagged}{Character vector of all variables flagged for
likelihood sensitivity.}
\item{sensitivity}{The original sensitivity object.}
}
}
\description{
Screen a \code{prior_sensitivity} result for parameters showing notable prior
sensitivity, likelihood sensitivity, or both. Parameters flagged for both
forms of sensitivity may indicate possible prior-data conflict in the
\pkg{priorsense} framework, while parameters flagged primarily for prior
sensitivity may reflect weak likelihood informativeness.
}
\details{
This helper is designed as a lightweight screening step after
\code{\link[=prior_sensitivity]{prior_sensitivity()}}. It does not replace graphical inspection. The
recommended workflow is:
\enumerate{
\item Run \code{prior_sensitivity()} to compute sensitivity.
\item Use \code{prior_sensitivity_conflict()} to identify parameters of
potential concern.
\item Re-run \code{prior_sensitivity()} with \code{plot = "dens"},
\code{plot = "ecdf"}, or \code{plot = "quantities"} for the
flagged variables.
}
}
\examples{
\donttest{
# Fit Bayesian SITAR model 

# To avoid model estimation which can take time, the Bayesian SITAR model fit
# to the 'berkeley_exdata' has been saved as an example fit ('berkeley_exfit').
# See 'bsitar' function for details on 'berkeley_exdata' and 'berkeley_exfit'.

model <- getNsObject(berkeley_exfit)

ps <- prior_sensitivity(
  model = model,
  variable = c("b_a_Intercept", "sigma")
)

chk <- prior_sensitivity_conflict(ps)
chk$conflict_params
chk$prior_only_params
}

}
\seealso{
\code{\link[=prior_sensitivity]{prior_sensitivity()}}
}
